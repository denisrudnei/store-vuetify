# Shop

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000 [server]
$ npm run dev:server

# serve with hot reload at localhost:3000 [client]
$ npm run dev:client

# build for production and launch server
$ npm run build
$ npm run start

```
